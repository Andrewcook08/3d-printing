"""Parametric 3D-printable parts, generated as STL.

Each subpackage is one 3D-printing project. Shared machinery lives here:
`stl` writes binary STL, `parts` defines what a printable part is and where its
file lands, and `cli` provides the `build` and `verify` commands.
"""
