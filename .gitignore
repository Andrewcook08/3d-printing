# Python
__pycache__/
*.py[cod]
.venv/
*.egg-info/

# Tooling caches
.pytest_cache/
.ruff_cache/
.ty_cache/
.coverage
htmlcov/

# macOS
.DS_Store

# Parts a project has stopped declaring, moved aside by `build`
output/archive/

# Parts still being tested -- built and checked, never committed
output/trials/

# Slicer projects and sliced output. The repo's job ends at the STL; how a
# given printer was told to make it is not a fact about the part, and the
# files are large and per-machine.
*.3mf
*.gcode
BambuProjects/
